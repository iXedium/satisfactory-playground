import React from 'react';
import { Modal as CommonModal } from '../common';

export type ModalProps = React.ComponentProps<typeof CommonModal>;

export const Modal = CommonModal;

export default Modal; 